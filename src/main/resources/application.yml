server:
  port: 18888

spring:
  application:
    name: agent-hub
  datasource:
    url: jdbc:mysql://8.140.249.192:3306/agent_hub?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USERNAME:admin}
    password: ${MYSQL_PASSWORD:78g*Tw23.Ysq}
    hikari:
      minimum-idle: 10
      maximum-pool-size: 50
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  http:
    timeout-seconds: 30
  scheduler:
    knowledge-ingest:
      enabled: true
      fixed-delay-ms: 3600000
  llm:
    enabled: true
    # OpenAI-compatible: /v1/chat/completions
    base-url: https://api.deepseek.com
    api-key: ${LLM_API_KEY:sk-e466e545fce74ab9adcdbdfdee9b8c37}
    model: deepseek-reasoner
    timeout-seconds: 300


alarm:
  lark:
    webhook: https://open.larksuite.com/open-apis/bot/v2/hook/038d5958-9bee-47a3-97e4-5d3a1be4c385
    secret: YfL2Cm12ZxW3gxy3kMmYDf
    enable: true

